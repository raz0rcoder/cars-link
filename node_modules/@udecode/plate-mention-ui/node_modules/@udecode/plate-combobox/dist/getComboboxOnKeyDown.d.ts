import { KeyboardHandler } from '@udecode/plate-core';
/**
 * If the combobox is open, handle:
 * - down (next item)
 * - up (previous item)
 * - escape (reset combobox)
 * - tab, enter (select item)
 */
export declare const getComboboxOnKeyDown: () => KeyboardHandler;
//# sourceMappingURL=getComboboxOnKeyDown.d.ts.map